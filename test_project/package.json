{
  "name": "test_project",
  "version": "1.0.0",
  "scripts": {
    "build": "rollup -c",
    "prebuild": "yarn clean",
    "clean": "rimraf dist/*"
  },
  "devDependencies": {
    "rimraf": "^3.0.0",
    "rollup": "^1.22.0"
  }
}
